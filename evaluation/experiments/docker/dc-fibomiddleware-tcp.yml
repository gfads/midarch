version: '3.1'
services:
  namingserver:
    image: midarch/fibomiddleware:namingserver-tcp
#    networks:
#      gmidarch:
#        aliases:
#          - namingserver
#    ports:
#      - "4040:4040"
    deploy:
      replicas: 1
      restart_policy:
        condition: none

  server:
    image: midarch/fibomiddleware:server-tcp
#    networks:
#      gmidarch:
#        aliases:
#          - server
#    ports:
#      - "2030:2030"
    deploy:
      replicas: 1
      restart_policy:
        condition: none

  client:
    image: midarch/fibomiddleware:client-tcp
    deploy:
      replicas: 1
      restart_policy:
        condition: none
    environment:
      FIBONACCI_PLACE: "12"
      SAMPLE_SIZE: "10"

#networks:
#  gmidarch: