version: "3.3"
services:
  server:
    image: <image.server>
    deploy:
      replicas: 1
      restart_policy:
        condition: none
      resources:
        limits:
          cpus: "1"
          memory: 256M
        reservations:
          cpus: "1"
          memory: 256M
    environment:
      CA_PATH: "/usr/src/app/examples/certs/myCA.pem"
      CRT_PATH: "/usr/src/app/examples/certs/server.pem"
      KEY_PATH: "/usr/src/app/examples/certs/server.key"       
      TIME_TO_START_SERVER: 13
      INJECTION_INTERVAL: <adaptation.interval>

  client:
    image: <image.client>
    deploy:
      replicas: 1
      restart_policy:
        condition: none
      resources:
        limits:
          cpus: "1"
          memory: 256M
        reservations:
          cpus: "1"
          memory: 256M
    environment:
      CA_PATH: "/usr/src/app/examples/certs/myCA.pem"    
      <specific.env.client>
      SAMPLE_SIZE: <sample.size>
      AVERAGE_WAITING_TIME: <average.waiting.time>
      TIME_TO_START_CLIENT: 18